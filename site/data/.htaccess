Options -Indexes

<IfModule mod_headers.c>
  Header always set Cache-Control "private, no-store, max-age=0"
</IfModule>

<FilesMatch "^(?!(catalog|encode-progress|content-index|content-analysis-progress|bunny-sync-status)\.json$|encode-preview\.jpg$).+">
  Require all denied
</FilesMatch>
